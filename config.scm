(sphere: "algorithm")
(dependencies:
 (list-extra
  (include
   (base: base-macros)
   (base: values-macros))
  (load
   (base: functional)
   (algorithm: srfi-1-list)))
 (random
  (load
   (algorithm: srfi-1-list))))
